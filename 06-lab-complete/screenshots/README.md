# Screenshots Folder

Thư mục này dùng để chứa các ảnh chụp màn hình (screenshots) minh chứng cho bài nộp Lab Day 12 theo yêu cầu của giảng viên trong [DAY12_DELIVERY_CHECKLIST.md](../DAY12_DELIVERY_CHECKLIST.md).

## Các ảnh cần chuẩn bị:

1. **dashboard.png**: 
   - Ảnh chụp màn hình trang quản trị Railway Dashboard của bạn (hiển thị rõ service `agent` và database `Redis` đang Online).
   
2. **running.png**:
   - Ảnh chụp màn hình hiển thị trạng thái Deploy thành công (Active/Online) của dịch vụ `agent` trên giao diện web Railway.

3. **test.png**:
   - Ảnh chụp màn hình terminal của bạn khi chạy các lệnh kiểm thử curl thành công tới Public URL (ví dụ lệnh gọi `/health` hoặc `/ask`).

*Lưu ý: Vui lòng lưu các ảnh chụp màn hình tương ứng với các tên file trên (định dạng `.png`) vào thư mục `screenshots/` này trước khi push lên GitHub để hoàn tất bài nộp.*
